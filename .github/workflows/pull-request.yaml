name: Pull Request Validation

on:
  pull_request:
    branches:
      - main

jobs:
  workflow:
    name: Reusable Workflow
    uses: LexisNexis-RBA/rsg-terraform-workflows/.github/workflows/module-pull-request.yaml@main
    secrets: inherit
    with:
      module_name: terraform-azurerm-aks
      terraform_version: 1.3.3
